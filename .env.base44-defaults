# Development defaults — NO real or guessable secrets here.
# Leave empty: the platform (Base44) injects real secrets via /run/base44/app.env,
# and local dev runs auth-open or sets its own LEAD_ENGINE_ADMIN_PASSWORD.
# The backend rejects placeholder/known-default passwords (auth.py guard).
LEAD_ENGINE_ADMIN_PASSWORD=
LEAD_ENGINE_AUTH_SECRET=