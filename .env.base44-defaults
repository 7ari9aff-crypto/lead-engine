# Development placeholders — overridden by /run/base44/app.env (platform secrets).
# These exist only so the app boots with auth enabled before real secrets land.
LEAD_ENGINE_ADMIN_PASSWORD=dev-admin-password
LEAD_ENGINE_AUTH_SECRET=dev-auth-secret-change-me
